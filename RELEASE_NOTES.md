# engineer 技能 v2.1 发布说明

> 版本：v2.1 · 日期：2026-08-21
> 仓库：https://github.com/TYYHeck/engineer-skill
> 说明：效率与 token 数据为**按典型场景的估算值**（旗舰档 30 点 / 10 模块项目为基准），实际以项目为准。

---

## v2.1 更新要点（本轮增量）

### 1. 批注交互增强：hover 双向高亮
- 鼠标悬停带 `data-ui` 元素 → 细边框提示；已有批注 → 主题色高亮边框；
- 悬停批注列表项 → 页面对应元素同步高亮——**所见即所批**；
- 批注方式改为**右键**（左键留给页面自身交互，对齐实际项目规范）。

### 2. AI 修改流程强制规则（不查看网页）
- **AI 全程不打开/不查看网页、不截图、不 DOM 探测**——只根据批注 `ui` 路径定位代码并精准 Edit；
- 完整闭环：**AI 生成预览 → 用户批注 → AI 按批注修改 → 用户查看**，AI 永远不需要看网页；
- 禁止重生成整页（Edit 增量改）、禁止截图定位。

### 3. UI 骨架定义（全局 → 局部）
- 骨架 = **整体背景色 → 分栏布局 → 每个页面细分设计**，自上而下；
- 逐组件循环新增 ⓪ 全局骨架层（背景/分栏/页面框架先定，再进组件级）。

### 4. git 集成加强（强制提交）
- 启用 git 后**每块/每批结束必须提交一次，无例外**，提交优先于块结束动作，遗忘即流程错误；
- commit 规范 `<type>(<scope>): <摘要>`，停滞/异常也提交。

---

## v2.0 更新要点（历史）

### 1. 文档体系三档分档（轻量 / 标准 / 旗舰）
- 轻量档：单文件 `project_plan.md` 内嵌状态，零嵌套负担（新增 `project_plan_lite_template.md`）；
- 标准档：完整体系（`project_plan.md` + `develop/<模块>/<点>.md` + `<点>/work.md`）；
- 旗舰档：标准档 + 每层 `index.md` + 模块级讨论记录 + 看板；
- 定档时机修正：移到**功能清单确认后**（交付质量 + 点数信号齐才推荐），只升不降；
- 轻量档提问裁剪：只问技术栈 + 交付质量，跳过部署 / 设计方式 / UI。

### 2. 状态机瘦身
- 7 态 → **5 态**（待设计/设计中/开发中/测试中/通过）+ 停滞；砍掉"待开发/待测试"等待态；
- **砍掉"任务类型"概念**（完整/设计/实现/验证/修复/重构）——建点只判"有无设计需求"定起点；
- work.md 保留可选「性质」字段（新功能/修复/重构），不参与流程判断；
- 停滞格式明确：`停滞（原因/阻塞点）`，解锁回原状态。

### 3. SKILL.md 瘦身为"决策器 + 路由器"
- 删除三块流程/状态机/提问浓缩版（消除与 references 的重复漂移）；
- 新增**必读清单** + "先读 references 再动手"硬约束；
- **触发词修正**：带宾语（"开发一个 XX"）→ 设计块；指向现有项目 → 开发块。

### 4. 通用入口 entry_flow.md（新增）
- 开发/测试块共用：模式三选 + 疑问处理三选**并行问**，范围仅模式②追加；
- 开发/测试块各自只保留特有步骤（消除重复）。

### 5. Token 经济性优化（重点）
- **work.md 状态降频**：每操作一步 → **每点结束更新一次**；
- **project_plan.md 批次回写**：每点回写 → **每批统一回写** + 批量变更日志；
- **work_template 瘦身**：YAML 7 字段 → 4（point/性质/platform/status）；
- **锚点操作规范**：同会话记忆直接 Edit 不重复 Read；Grep 锚点核验替代全文 Read；跨会话优先头部核验；
- **提问默认合并**：能一批必一批；
- **UI 预览落盘化**：对话内全量渲染 → `ui-preview/` 落盘 + Edit 增量迭代。

### 6. UI 可视化机制重构
- 预览**落盘成网页文件**（每页 HTML + theme.css + 批注脚本），迭代增量改；
- **批注闭环**：元素埋 `data-ui` 锚点 → 点击弹批注条 → 攒批注 → 一键复制按页分组文本 → AI 精准修改；
- 控制面板保留不降级（调参）+ 批注（定位）互补；
- **精细档逐组件设计循环**（硬约束）：组件 → 页面组合 → 全局回归 三层逐级确认；收尾必须给 UI 确认清单，未完成不得进开发块；
- 两次预览职责划分：设计块定风格（原型资产），开发块引用 theme.css 不重复预览。

### 7. 补齐缺失环节
- 收尾总结段（完成情况/遗留问题/如何运行）+ 触发条件（全通过/验收/说完成）；
- 变更日志段（批次回写时批量追加）；
- 跨会话续作动作（读 plan → 找未完成点 → work.md → 继续）；
- platform 枚举补 `desktop`（web/desktop/mobile/mini）。

### 8. git 集成（新增）
- 设计块开头问"需要 git 吗"；需要则 init + .gitignore + 初始 commit；
- 每块/每批结束提交一次，commit 规范 `<type>(<scope>): <摘要>`。

## 二、效率增强（估算）

| 项 | 旧 | 新 | 提升 |
|---|---|---|---|
| 文档写操作次数（30 点项目） | ~180 次 | ~36 次 | **↓ 80%** |
| 提问交互轮次 | 串行 4 连问 × 多轮 | 并行一批问 | **↓ ~40%** |
| UI 迭代反馈 | 文字描述位置，AI 脑补 | 批注精确定位，一次改多处 | **迭代次数 ↓ ~30-50%** |
| UI 返工 | 开发后改样式 | 设计时逐组件定稿 | **返工率 ↓ ~50%** |
| 综合效率（旗舰档全流程） | — | — | **↑ ~40-60%** |

## 三、Token 节省（估算）

| 项 | 旧（对话内渲染） | 新（落盘增量） | 节省 |
|---|---|---|---|
| UI 预览（5 页逐页） | 6-8 万 | 2-3 万 | **↓ ~60-70%** |
| UI 预览（IDE 10+ 页） | 15-25 万 | 5-8 万 | **↓ ~60-70%** |
| 文档读写 | 每步落盘 + 每点回写 | 每点一次 + 批次回写 + 锚点 | **↓ ~50-70%** |
| 技能加载/去重 | references 含重复 | entry_flow 合并去重 | **↓ ~10%** |
| **旗舰档全流程综合** | — | — | **↓ ~50-60%** |

## 四、文件清单（15 文件 = 14 技能文件 + 本说明）

```
engineer/
├── SKILL.md                                # 决策器 + 必读路由 + git 集成
├── RELEASE_NOTES.md                        # 本说明
├── references/                             # 8 个
│   ├── entry_flow.md                       # 新增：通用入口（模式/疑问/范围）
│   ├── doc_system.md                       # 三档分档 + 续作动作
│   ├── state_machine.md                    # 5 态状态机 + 锚点操作规范
│   ├── design_block.md                     # 定档后移 + git 询问 + UI 硬护栏
│   ├── develop_block.md                    # 引用 entry_flow + 批次回写
│   ├── test_block.md                       # 引用 entry_flow + 批次回写
│   ├── questioning.md                      # 选项规范 + 默认合并
│   └── ui_design.md                        # 落盘预览 + 批注闭环 + 逐组件循环
└── assets/                                 # 5 个
    ├── project_plan_template.md            # 标准/旗舰（收尾总结 + 变更日志）
    ├── project_plan_lite_template.md       # 新增：轻量档单文档
    ├── point_template.md                   # 点主文档模板
    ├── work_template.md                    # YAML 4 字段瘦身版
    └── mode_template.md                    # 模式配置
```

---

*估算口径：旗舰档 30 点 / 10 模块 / 多轮 UI 迭代场景；轻量档节省幅度小于旗舰档，但流程负担也最小。*
